import SwiftUI

struct ScheduleView: View {
    var body: some View {
        ComingSoonView(
            symbol: "calendar",
            title: "Schedule",
            subtitle: "Plan study sessions and get reminders."
        )
    }
}
