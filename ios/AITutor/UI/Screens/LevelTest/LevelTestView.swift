import SwiftUI

struct LevelTestView: View {
    var body: some View {
        ComingSoonView(
            symbol: "chart.bar.fill",
            title: "Level Test",
            subtitle: "10-minute placement test → CEFR estimate (A1–C2)."
        )
    }
}
