import SwiftUI

/// Centralized design tokens. Mirrors the Compose `AITutorTheme` in shape so
/// porting between platforms stays mechanical.
enum Theme {
    enum Colors {}
    enum Typography {}
    enum Shape {}
}
